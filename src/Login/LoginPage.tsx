const LoginPage = () => {
	//Egyenlőre csak a regisztrált felhasználókat mutatja
	return (
		<div>
			<h1>Login</h1>
			{/* {users.map((user, index) => {
				return <p key={`user-${index}`}>{`${user.name} - ${user.age}`}</p>;
			})} */}
		</div>
	);
};

export default LoginPage;
